package main

import (
	"fmt"
)

// The main entry point is at the project root (main.go).
// This file exists for Go project layout convention.
// Build with: go build -o viewer .
func main() {
	fmt.Println("Build from project root: go build -o viewer .")
}
